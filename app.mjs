import {selectProfiles} from './engine.mjs';
import {data,options,scenarios,explanation} from './catalog.mjs';
const $=s=>document.querySelector(s);
const escapeHTML=value=>String(value).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
for(const [id,key] of [['city','cities'],['category','categories'],['format','formats'],['language','languages']]){const select=$('#'+id);select.replaceChildren(...(id==='language'?[new Option('Любой','')]:[]),...options[key].map(v=>new Option(v,v)));}
function applyScenario(name){for(const [key,value] of Object.entries(scenarios[name]||scenarios.popular))$('#'+key).value=value;}
applyScenario('popular');
const check='<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" aria-hidden="true"><path d="m5 12 4 4L19 6"/></svg>';
const icon='<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true"><path d="M7 5h14M7 12h10M7 19h14M2 5h1M2 12h1M2 19h1"/></svg>';
let requestId=0,lastQuery=null;
const money=n=>new Intl.NumberFormat('ru-RU').format(n);
const dateLabel=d=>new Date(d+'T12:00:00').toLocaleDateString('ru-RU',{day:'numeric',month:'long'});
function query(){const f=new FormData($('#search'));return Object.fromEntries([...f].map(([k,v])=>[k,['budget','hours'].includes(k)?Number(v):v]));}
function empty(title,body,actions='',extra=''){return `<div class="empty"><div class="empty-symbol">${icon}</div><h3>${title}</h3><p>${body}</p>${extra}<div>${actions}</div></div>`;}
function initial(){setFormExpanded(true);$('#edit-conditions').hidden=true;$('#conditions-summary').hidden=true;requestId++;lastQuery=null;$('#submit').disabled=false;$('#submit').innerHTML='Подобрать подрядчиков <span aria-hidden="true">→</span>';$('#results').removeAttribute('aria-busy');$('#count').hidden=true;$('#dirty').hidden=true;$('#price-note').hidden=true;$('#results-title').textContent='Ваш короткий список';$('#results-subtitle').textContent='Сначала расскажите о мероприятии';$('#result-content').innerHTML=empty('Найдём тех, кто вам подходит','Проверьте условия мероприятия — мы соберём до трёх вариантов и объясним, чем полезен каждый.','','<div class="empty-steps"><span><b>01</b> Ваши условия</span><span><b>02</b> Свободные по календарю</span><span><b>03</b> Понятный выбор</span></div>');}
function validate(){let valid=true;const q=query();for(const id of ['date','budget','hours']){let error='';if(id==='date'&&(!q.date||q.date<'2026-09-23'||q.date>'2026-12-31'))error='Выберите дату с 23 сентября по 31 декабря 2026.';if(id==='budget'&&(!Number.isFinite(q.budget)||q.budget<=0))error='Укажите бюджет больше нуля.';if(id==='hours'&&(!Number.isFinite(q.hours)||(document.querySelector('#hours').value!==''&&q.hours<1)||q.hours>24||!Number.isInteger(q.hours)))error='Укажите от 1 до 24 целых часов или оставьте поле пустым.';$('#'+id+'-error').textContent=error;$('#'+id).setAttribute('aria-invalid',String(!!error));$('#'+id).setAttribute('aria-describedby',id+'-error');if(error){setFormExpanded(true);if(id==='hours')$('#extra').open=true;if(valid)$('#'+id).focus();valid=false;}}return valid;}
const reasonLabels={busy:'заняты на выбранную дату',budget:'стартовая цена выше бюджета',format:'не работают с этим форматом',language:'не работают на выбранном языке',hours:'не подходят по длительности'};
function card(p,q){
 const text=explanation(p,q);
 const split=text.indexOf(' Из описания:');
 const matchText=split<0?text:text.slice(0,split);
 const quoteText=split<0?'':text.slice(split+1);
 const provenance=p.synthetic?'Синтетический профиль':'Анонимизированный профиль';
 const notes=[provenance,p.priceImputed?'Цена проставлена при подготовке датасета':'',p.cityImputed?'Город проставлен при подготовке датасета':''].filter(Boolean);
 return `<article class="vendor" data-profile-id="${escapeHTML(p.id)}"><div class="vendor-top"><div class="avatar" aria-hidden="true">${escapeHTML(p.initials)}</div><div class="vendor-title"><h3>${escapeHTML(p.name)}</h3><div class="vendor-meta">${escapeHTML(q.category)} · ${escapeHTML(p.city)}</div></div><div class="price"><div><small>от</small> ${money(p.price)} ₸${p.priceImputed?' *':''}</div><div class="available">Свободен по календарю · ${dateLabel(q.date)}</div></div></div><div class="facts"><span>${check}${escapeHTML(q.format)}</span><span>${check}${escapeHTML(p.languages.join(' / '))}</span><span>${check}${p.hours===null?'Без привязки к часам на площадке':'До '+p.hours+' часов'}</span></div><div class="explanation"><strong>Почему подходит</strong><p class="match-copy">${escapeHTML(matchText)}</p><p class="source-copy">${escapeHTML(quoteText)}</p></div><p class="provenance">${escapeHTML(notes.join(' · '))}</p><details><summary>Подробнее о подрядчике <span aria-hidden="true">+</span></summary><p>${escapeHTML(p.detail)}<br>Категории: ${escapeHTML(p.categories.join(', '))}.<br>Форматы: ${escapeHTML(p.formats.join(', '))}.<br>Профиль ${escapeHTML(p.id)} из датасета хакатона. Имя вымышленное.</p></details></article>`;
}
async function run(fail=false){if(!validate())return;const ticket=++requestId;const q=query();$('#submit').disabled=true;$('#submit').textContent='Подбираем…';$('#results').setAttribute('aria-busy','true');$('#dirty').hidden=true;$('#count').hidden=true;$('#price-note').hidden=true;$('#results-title').textContent='Подбираем подрядчиков';$('#results-subtitle').textContent=`${q.city} · ${dateLabel(q.date)} · ${q.format}`;$('#result-content').innerHTML='<div class="empty"><div class="loader" aria-hidden="true"></div><h3>Проверяем ваши условия</h3><p>Сопоставляем дату, бюджет и пожелания с профилями из датасета хакатона.</p></div>';await new Promise(r=>setTimeout(r,600));if(ticket!==requestId)return;lastQuery=q;$('#submit').disabled=false;$('#submit').innerHTML='Подобрать подрядчиков <span aria-hidden="true">→</span>';$('#results').removeAttribute('aria-busy');finishSearch(q);if(fail){$('#results-title').textContent='Подбор не завершён';$('#result-content').innerHTML=empty('Не удалось получить результат','Это демонстрация технического сбоя. Все параметры сохранены — попробуйте ещё раз.','<button class="text-button" data-action="retry">Повторить подбор</button>');return;}const r=selectProfiles(data,q);if(r.state==='success'){$('#results-title').textContent='Подходят вашему событию';$('#count').hidden=false;$('#count').textContent=`${r.items.length} из ${r.total}`;$('#price-note').hidden=false;const why=Object.entries(r.reasons).filter(([,n])=>n).map(([k,n])=>`${n} — ${reasonLabels[k]}`).join('; ');$('#result-content').innerHTML=(r.total<3?`<div class="notice">Подходящих вариантов: ${r.total}. ${r.pool===r.total?'В этой категории и городе всего '+r.pool+' в датасете.':'Остальные не прошли условия: '+why+'.'} Показываем всех, кто подходит.</div>`:'')+r.items.map(p=>card(p,q)).join('');}else if(r.state==='absent'){$('#results-title').textContent='Категория пока не представлена';$('#result-content').innerHTML=empty('В этом городе такой категории нет',`В датасете нет категории «${q.category}» для города ${q.city}. Дата и бюджет не повлияли на этот результат.`,'<button class="text-button" data-focus="category">Изменить категорию</button><button class="text-button" data-focus="city">Изменить город</button>');}else{$('#results-title').textContent='По этим условиям вариантов нет';const entries=Object.entries(r.reasons).filter(([,n])=>n);const list='<ul class="reason-list">'+entries.map(([k,n])=>`<li>${n} — ${reasonLabels[k]}</li>`).join('')+'</ul>';const fields={busy:['date','Изменить дату'],budget:['budget','Изменить бюджет'],format:['format','Изменить формат'],language:['language','Изменить язык'],hours:['hours','Изменить длительность']};$('#result-content').innerHTML=empty('Подрядчики есть, совпадений нет',`В городе ${q.city} профилей в категории «${q.category}»: ${r.pool}. Ни один не проходит все условия. У одного профиля может быть несколько причин.`,entries.map(([k])=>`<button class="text-button" data-focus="${fields[k][0]}">${fields[k][1]}</button>`).join(''),list);}$('#dirty').hidden=JSON.stringify(query())===JSON.stringify(lastQuery);}
function setFormExpanded(expanded){
 $('.filter-panel').classList.toggle('form-collapsed',!expanded);
 $('#edit-conditions').setAttribute('aria-expanded',String(expanded));
 $('#edit-conditions').textContent=expanded?'Свернуть':'Изменить';
}
function finishSearch(q){
 $('#edit-conditions').hidden=false;
 $('#conditions-summary').textContent=`${q.city} · ${dateLabel(q.date)} · ${q.format} · ${q.category} · до ${money(q.budget)} ₸`;
 $('#conditions-summary').hidden=false;
 if(matchMedia('(max-width: 920px)').matches){setFormExpanded(false);$('#results').scrollIntoView({block:'start'});$('#results').focus({preventScroll:true});}
}
$('#edit-conditions').addEventListener('click',()=>{const expanded=$('#edit-conditions').getAttribute('aria-expanded')!=='true';setFormExpanded(expanded);if(expanded){$('.filter-panel').scrollIntoView({block:'start'});$('#city').focus({preventScroll:true});}});
$('#search').addEventListener('submit',e=>{e.preventDefault();run();});$('#search').addEventListener('input',()=>{document.querySelectorAll('[data-scenario]').forEach(b=>b.setAttribute('aria-pressed','false'));if(lastQuery)$('#dirty').hidden=JSON.stringify(query())===JSON.stringify(lastQuery);});
document.addEventListener('click',e=>{const focus=e.target.closest('[data-focus]');if(focus){const id=focus.dataset.focus;setFormExpanded(true);if(['hours','language'].includes(id))$('#extra').open=true;$('#'+id).focus();$('#'+id).scrollIntoView({block:'center'});}if(e.target.closest('[data-action="retry"]'))run();const button=e.target.closest('[data-scenario]');if(!button)return;document.querySelectorAll('[data-scenario]').forEach(b=>b.setAttribute('aria-pressed',String(b===button)));const name=button.dataset.scenario;applyScenario(name);for(const id of ['date','budget','hours']){$('#'+id+'-error').textContent='';$('#'+id).removeAttribute('aria-invalid');}if(name==='initial'){initial();$('.filter-panel').scrollIntoView({block:'start'});return;}run(name==='error');$('#results').scrollIntoView({block:'start'});});
initial();
if(document.modelContext?.registerTool){
 const lifecycle=new AbortController();
 try{Promise.resolve(document.modelContext.registerTool({name:'run_current_contractor_search',title:'Подобрать подрядчиков по текущей форме',description:'Проверяет текущие поля, выполняет демонстрационный подбор и обновляет видимые результаты.',inputSchema:{type:'object',properties:{},additionalProperties:false},annotations:{readOnlyHint:false,untrustedContentHint:false},async execute(input){if(!input||typeof input!=='object'||Array.isArray(input)||Object.keys(input).length)throw new Error('Ожидается пустой объект');if(!validate())throw new Error('Исправьте ошибки формы');await run();const result=selectProfiles(data,query());return {state:result.state,names:result.items.map(p=>p.name)};}},{signal:lifecycle.signal})).catch(()=>{});}catch{}
 addEventListener('pagehide',()=>lifecycle.abort(),{once:true});
}
